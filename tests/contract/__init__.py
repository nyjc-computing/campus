"""tests.contract

HTTP contract tests for Campus API endpoints.

These tests verify the HTTP interface contract (status codes, response
formats, authentication behavior) without testing internal implementation.
"""
